import React from 'react';
import { Plus, Trash2 } from 'lucide-react';
import ETAFormField, { etaInput } from './ETAFormField';
export default function ETACursorSteps({ value=[], onChange, frameDurations=false, defaults={} }) { const set=(i,key,val)=>onChange(value.map((item,n)=>n===i?{...item,[key]:val}:item)); return <div className="space-y-3"><div className="flex items-center justify-between"><div><h3 className="text-xs font-semibold">Cursor steps</h3><p className="text-[11px] text-muted-foreground">{value.map(x=>`${Number(x.time).toFixed(1)}s`).join('  ')}</p></div><button onClick={()=>onChange([...value,{time:0,selector:'',duration:frameDurations?18:.5,click:true,clickDuration:frameDurations?8:.15,hideAfterClick:false,...defaults}])} className="flex items-center gap-1 rounded-lg border border-border px-3 py-2 text-xs"><Plus className="h-3 w-3"/> Add</button></div>{value.map((item,i)=><div key={i} className="grid gap-3 rounded-xl border border-border p-3 sm:grid-cols-2"><ETAFormField label="Start time (s)"><input className={etaInput} type="number" step=".1" value={item.time} onChange={(e)=>set(i,'time',Number(e.target.value))}/></ETAFormField><ETAFormField label="Target selector"><input className={etaInput} value={item.selector||''} onChange={(e)=>set(i,'selector',e.target.value)}/></ETAFormField><ETAFormField label={`Duration (${frameDurations?'frames':'s'})`}><input className={etaInput} type="number" step=".1" value={item.duration} onChange={(e)=>set(i,'duration',Number(e.target.value))}/></ETAFormField><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={item.click!==false} onChange={(e)=>set(i,'click',e.target.checked)}/> Click</label><ETAFormField label={`Click duration (${frameDurations?'frames':'s'})`}><input className={etaInput} type="number" step=".1" value={item.clickDuration??0} onChange={(e)=>set(i,'clickDuration',Number(e.target.value))}/></ETAFormField>{frameDurations&&<label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={Boolean(item.hideAfterClick)} onChange={(e)=>set(i,'hideAfterClick',e.target.checked)}/> Hide after click</label>}<button onClick={()=>onChange(value.filter((_,n)=>n!==i))} className="flex items-center gap-1 text-xs text-destructive"><Trash2 className="h-3 w-3"/> Delete</button></div>)}</div>; }