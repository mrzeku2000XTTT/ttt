import React from 'react';
import ETAZoomKeyframes from './ETAZoomKeyframes';
const defaults=[{time:0,selector:'',x:0,y:0,scale:1,duration:'default',easing:'custom'},{time:.8,selector:'.main-panel',x:0,y:0,scale:1.6,duration:'default',easing:'custom'},{time:2,selector:'',x:0,y:0,scale:1,duration:'default',easing:'custom'}];
export default function ETAUIAnimationSettings({ advanced, setAdvanced }) { const zooms=advanced.zoomKeyframes?.length?advanced.zoomKeyframes:defaults; return <section className="space-y-4 rounded-2xl border border-border bg-card p-4"><div><h2 className="text-sm font-semibold">UI animation</h2><p className="mt-1 text-[11px] text-muted-foreground">Camera zoom uses CSS selectors on the generated UI (e.g. .panel). Empty selector zooms out or uses X/Y.</p></div><ETAZoomKeyframes value={zooms} onChange={(next)=>setAdvanced('zoomKeyframes',next)} defaults={{selector:'.main-panel'}}/><p className="text-[11px] text-muted-foreground">Drag to scrub. Hold Shift for larger steps, Alt for finer steps.</p></section>; }