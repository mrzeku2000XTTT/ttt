import React from 'react';
import { ETA_COMPONENTS } from '@/lib/etaPlan';
import ETAFormField, { etaInput } from './ETAFormField';

export default function ETAAdvancedBasics({ scene, setScene, advanced, setAdvanced }) {
  return <section className="space-y-4 rounded-2xl border border-border bg-card p-4"><h2 className="text-sm font-semibold">Component</h2><div className="grid gap-3 sm:grid-cols-2"><ETAFormField label="Component"><select className={etaInput} value={scene.component} onChange={(e) => setScene({ ...scene, component: e.target.value })}>{ETA_COMPONENTS.map((item) => <option key={item}>{item}</option>)}</select></ETAFormField><ETAFormField label="Duration (s)"><input className={etaInput} type="number" min="1" step=".5" value={scene.duration} onChange={(e) => setScene({ ...scene, duration: Number(e.target.value) })} /></ETAFormField><ETAFormField label="Title"><input className={etaInput} value={scene.headline} onChange={(e) => setScene({ ...scene, headline: e.target.value })} /></ETAFormField><ETAFormField label="Subtitle"><input className={etaInput} value={advanced.subtitle || ''} onChange={(e) => setAdvanced('subtitle', e.target.value)} /></ETAFormField><ETAFormField label="Search text"><input className={etaInput} value={advanced.searchText || ''} onChange={(e) => setAdvanced('searchText', e.target.value)} placeholder="Search text" /></ETAFormField></div></section>;
}