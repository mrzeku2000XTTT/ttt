import React from 'react';
import ETAFormField, { etaInput } from './ETAFormField';

export default function ETACardsHeadlineControls({ advanced, setAdvanced }) {
  const set = (key, value) => setAdvanced(key, value);
  return <div className="grid gap-3 sm:grid-cols-2"><ETAFormField label="Center headline"><input className={etaInput} value={advanced.centerHeadline || ''} placeholder="e.g. stepping up" onChange={(e) => set('centerHeadline', e.target.value)} /></ETAFormField><ETAFormField label="Color"><input className={etaInput} type="color" value={advanced.cardColor || '#171a1f'} onChange={(e) => set('cardColor', e.target.value)} /></ETAFormField><ETAFormField label="Font family"><select className={etaInput} value={advanced.cardFont || 'SF Pro Display'} onChange={(e) => set('cardFont', e.target.value)}><option>SF Pro Display</option><option>Space Grotesk</option><option>Fraunces</option><option>Inter</option></select></ETAFormField><ETAFormField label="Font size (px)"><input className={etaInput} type="number" value={advanced.cardFontSize ?? 28} onChange={(e) => set('cardFontSize', Number(e.target.value))} /></ETAFormField><ETAFormField label="Font weight"><input className={etaInput} type="number" min="100" max="900" step="100" value={advanced.cardFontWeight ?? 700} onChange={(e) => set('cardFontWeight', Number(e.target.value))} /></ETAFormField><ETAFormField label="Text transform"><select className={etaInput} value={advanced.textTransform || 'As typed'} onChange={(e) => set('textTransform', e.target.value)}><option>As typed</option><option>Uppercase</option><option>Lowercase</option></select></ETAFormField>{[['Card corner radius (px)','cardRadius',16],['Card width (px)','cardWidth',112],['Card height (px)','cardHeight',160]].map(([label,key,fallback]) => <ETAFormField key={key} label={label}><input className={etaInput} type="number" value={advanced[key] ?? fallback} onChange={(e) => set(key, Number(e.target.value))} /></ETAFormField>)}</div>;
}