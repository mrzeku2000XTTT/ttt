import React from 'react';
import { Plus, Trash2 } from 'lucide-react';
import ETAFormField, { etaInput } from './ETAFormField';

const fresh = () => ({ time: 0, content: '', amplitude: 8, speed: 1, x: 0, y: 0, z: 0, offset: 0, rotate: true });
export default function ETABrowserKeyframes({ value = [], onChange }) {
  const set = (i, key, next) => onChange(value.map((item, index) => index === i ? { ...item, [key]: next } : item));
  return <section className="rounded-2xl border border-border bg-card p-4"><div className="mb-4 flex items-center justify-between"><div><h2 className="text-sm font-semibold">Keyframes</h2><p className="text-xs text-muted-foreground">Browser window keyframes</p></div><button onClick={() => onChange([...value, fresh()])} className="flex items-center gap-1 rounded-lg border border-border px-3 py-2 text-xs"><Plus className="h-3 w-3" /> Add</button></div><div className="space-y-3">{value.length === 0 && <p className="text-xs text-muted-foreground">No browser keyframes yet.</p>}{value.map((item, i) => <div key={i} className="rounded-xl border border-border p-3"><div className="grid gap-3 sm:grid-cols-2"><ETAFormField label="At time (s)"><input className={etaInput} type="number" step=".1" value={item.time} onChange={(e) => set(i, 'time', Number(e.target.value))} /></ETAFormField><ETAFormField label="Content (URL)"><input className={etaInput} value={item.content} onChange={(e) => set(i, 'content', e.target.value)} /></ETAFormField>{[['Rotate amplitude','amplitude'],['Animation speed','speed'],['Rotate X','x'],['Rotate Y','y'],['Rotate Z','z'],['Start offset (s)','offset']].map(([label,key]) => <ETAFormField key={key} label={label}><input className={etaInput} type="number" step=".1" value={item[key]} onChange={(e) => set(i, key, Number(e.target.value))} /></ETAFormField>)}</div><div className="mt-3 flex items-center justify-between"><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={item.rotate} onChange={(e) => set(i, 'rotate', e.target.checked)} /> Rotate</label><button onClick={() => onChange(value.filter((_, index) => index !== i))} className="text-destructive"><Trash2 className="h-4 w-4" /></button></div></div>)}</div></section>;
}