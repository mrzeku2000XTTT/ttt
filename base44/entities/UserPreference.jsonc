{
  "name": "UserPreference",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "User identifier"
    },
    "interests": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "User's topics of interest (KAS, DeFi, NFTs, etc.)"
    },
    "liked_content": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "content_id": {
            "type": "string"
          },
          "content_type": {
            "type": "string"
          },
          "timestamp": {
            "type": "string"
          }
        }
      },
      "description": "Content user has liked"
    },
    "conversation_history_summary": {
      "type": "string",
      "description": "AI-generated summary of user's conversation patterns"
    },
    "preferred_topics": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Topics user engages with most"
    },
    "ai_personality_preference": {
      "type": "string",
      "enum": [
        "professional",
        "casual",
        "technical",
        "matrix"
      ],
      "default": "professional",
      "description": "User's preferred AI interaction style"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional description (up to 1000 characters)"
    }
  },
  "required": [
    "user_email"
  ],
  "rls": {
    "create": {
      "data.user_email": "{{user.email}}"
    },
    "read": {
      "data.user_email": "{{user.email}}"
    },
    "update": {
      "data.user_email": "{{user.email}}"
    },
    "delete": {
      "data.user_email": "{{user.email}}"
    }
  }
}