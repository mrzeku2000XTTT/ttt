{
  "name": "AppRoadmap",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "User who created this roadmap (empty for guests)"
    },
    "app_idea": {
      "type": "string",
      "description": "Original app idea input"
    },
    "enhanced_idea": {
      "type": "string",
      "description": "AI-enhanced version of the idea"
    },
    "roadmap_steps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "phase": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "tasks": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "completed": {
            "type": "boolean"
          }
        }
      },
      "description": "Array of roadmap phases with tasks"
    },
    "completed_tasks": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "default": [],
      "description": "Indices of completed steps (legacy)"
    },
    "completed_subtasks": {
      "type": "object",
      "description": "Map of 'stepIndex-taskIndex' to boolean for individual sub-task completion",
      "additionalProperties": {
        "type": "boolean"
      },
      "default": {}
    },
    "progress_percentage": {
      "type": "number",
      "default": 0,
      "description": "Overall completion percentage"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional description (up to 1000 characters)"
    }
  },
  "required": [
    "app_idea"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "data.user_email": ""
        },
        {
          "data.user_email": null
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "data.user_email": ""
        },
        {
          "data.user_email": null
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "data.user_email": ""
        },
        {
          "data.user_email": null
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "data.user_email": ""
        },
        {
          "data.user_email": null
        }
      ]
    }
  }
}